apiVersion: v1
kind: Service
metadata:
  name: {{ include "detaaltoolbox.fullname" . }}
  labels:
    {{- include "detaaltoolbox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "detaaltoolbox.selectorLabels" . | nindent 4 }}
